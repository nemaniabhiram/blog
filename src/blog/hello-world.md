---
title: "Hello, world"
description: "My very first post."
pubDate: 2026-06-17
---

This is my first post — testing all the markdown features.

## A heading

Some **bold text**, some *italic*, and a bit of `inline code`.

> A blockquote, for when you want to quote something.

A code block, which should pick up the Zed colors:

```js
function greet(name) {
  return `Hello, ${name}!`;
}
greet("world");
```

A table:

| Feature | Works? |
| ------- | ------ |
| Tables  | yes    |
| Code    | yes    |
| Images  | yes    |